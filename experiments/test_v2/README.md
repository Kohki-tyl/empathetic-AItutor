# Test v2

固定した未SFT生徒モデルと独立したJudgeを使用し、教師モデルだけを変更するプロファイル更新テストの結果を保存する。

条件フォルダー名には、`baseline`または`sft_vN`、モデル名、モデル規模、ベースモデル版を含める。

Phase 2では、Phase 1終了時の学習状態で生徒プロファイルを更新し、対話全文は渡さない。

本実験では教師モデル条件ごとのフォルダーを作成する。
